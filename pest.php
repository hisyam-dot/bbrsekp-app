<?php

use Pest\Plugin;

Plugin::load('pestphp/pest');

uses()->group('unit');

beforeEach(function () {
    // Setup code before each test
});

afterEach(function () {
    // Cleanup code after each test
});